// Mock for Joplin's `api` import. Real plugin code that imports `api` won't
// be unit-testable in isolation; tests should focus on pure helpers that don't
// touch this module. This stub exists so any incidental import resolves.
export default {};
