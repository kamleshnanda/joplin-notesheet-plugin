{
  "name": "joplin-plugin-univer-worksheet-plugin",
  "version": "2.4.0",
  "scripts": {
    "dist": "NODE_OPTIONS='--openssl-legacy-provider' webpack --env.joplin-plugin-config=buildMain && NODE_OPTIONS='--openssl-legacy-provider' webpack --env.joplin-plugin-config=buildExtraScripts && NODE_OPTIONS='--openssl-legacy-provider' webpack --env.joplin-plugin-config=createArchive",
    "prepare": "npm run dist",
    "updateVersion": "NODE_OPTIONS='--openssl-legacy-provider' webpack --env.joplin-plugin-config=updateVersion",
    "update": "npm install -g generator-joplin && yo joplin --node-package-manager npm --update --force"
  },
  "license": "MIT",
  "keywords": [
    "joplin-plugin"
  ],
  "files": [
    "publish"
  ],
  "devDependencies": {
    "@types/node": "^18.7.13",
    "chalk": "^4.1.0",
    "copy-webpack-plugin": "^4.2.3",
    "fs-extra": "^10.1.0",
    "glob": "^8.0.3",
    "tar": "^7.5.9",
    "ts-loader": "^8.4.0",
    "typescript": "^4.8.2",
    "webpack": "^4.25.1",
    "webpack-cli": "^3.3.12"
  }
}
